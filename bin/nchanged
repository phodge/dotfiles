#!/usr/bin/env bash
set -e -o pipefail

mastername=$(git-master-name)

other="origin/$mastername"

echo "Comparing with '$other' ..."
nvim "+Shell git diff $other... --name-status --no-renames; git status --short --untracked-files=all" "+bw 1"
